# AGENTS.md

本文件适用于整个仓库，用于让新的 Codex 会话快速理解项目边界、长期业务规则和开发约束。当前阶段进度、近期修改和待观察问题见 `docs/PROJECT_STATUS.md`。

## 1. 项目定位

这是一个面向酒店运营的多平台数据采集与运营自动化项目。

主要数据来源：

- 别样红 PMS
- 美团 OTA
- 携程 OTA

主要链路：

`PMS / 美团 / 携程 -> 自动采集 -> 数据转换/清洗 -> MySQL -> 房型/商品映射 -> 定时任务 -> 运营分析与调价辅助`

项目同时提供本地管理面板、登录辅助、周期调度、日志、房型映射，以及美团/携程调价任务相关能力。

## 2. 新会话开始时先做什么

处理任何项目任务前：

1. 先阅读本文件和 `docs/PROJECT_STATUS.md`。
2. 查看当前 `master` 最新提交，不要假设文档中的提交号仍是 HEAD。
3. 根据任务读取当前源文件；不要仅凭历史上下文修改代码。
4. 如果实现代码与进度文档冲突，以当前代码作为“实现事实”；如果与本文件中的明确业务规则冲突，先向用户确认，不要擅自改业务语义。
5. 修改前先明确预计涉及哪些文件，修改后核对实际 diff，避免扩大范围。

## 3. 核心目录

### `OTA采集服务/`

统一管理、调度、配置、数据库 Schema 和房型映射。

重点文件：

- `runner.py`：统一任务注册和执行入口。
- `database/create_all_tables.sql`：当前数据库 Schema 的主要事实来源。
- `mapping_product_sync.py`：OTA 商品与统一房型映射同步。
- `config/settings.example.json`：脱敏配置示例。

### `美团OTA数据采集代码/`

美团经营、流量、评价、活动、商品价格、推广、视频状态等采集代码，以及相关调价能力。

### `携程OTA数据采集代码/`

携程经营、竞争、订单、流量、评价、活动、商品价格等采集代码，以及相关调价能力。

### `正式数据抓取-PMS（别样红）/PMS登录/`

PMS 登录、报表采集和 ETL。

重点：

- `fetch_main.py`：PMS 全量采集/ETL 主入口。
- `scripts/`：各 PMS 报表采集器。
- `scripts/etl-mysql/`：PMS ETL。

### 根目录公共模块

- `ota_mysql_writer.py`：OTA 公共 MySQL 写入能力。
- `mysql_connection.py`：数据库连接辅助。

## 4. 事实来源优先级

遇到文档与实现不一致时，按下面方式判断：

- 数据库结构：优先查看 `OTA采集服务/database/create_all_tables.sql`。
- 调度任务清单：优先查看 `OTA采集服务/runner.py` 中的 `TASKS`。
- 接口、页面和采集规则：优先查看对应平台当前采集脚本。
- 长期业务约束：本 `AGENTS.md` 中明确标记的规则属于有意设计，不应仅因“看起来可以优化”就改掉。

## 5. 重要业务规则：不要随意改变

### 单酒店数据库边界

当前实际部署方式是一个数据库对应一家酒店。不要为了假设的多酒店共享数据库场景重构当前核心表、唯一键或任务边界，除非用户明确提出该需求。

### 美团钟点房判断保持两个独立来源

目前存在两个独立判断：

1. `meituan_ota_goods_price_mapping.is_hour_room`：商品采集脚本根据 `goodsName` 独立判断。
2. `hotel_room_type_mapping.is_hour_room`：`mapping_product_sync.py` 根据 OTA 商品名称独立判断。

当前核心匹配形式为：

```text
-[0-9]+([.][0-9]+)?小时-
```

不要把房型映射逻辑改成直接继承商品表的 `is_hour_room`，除非用户明确要求。

### PMS FORECAST / ROOM_STATUS 时间窗口

PMS 预测接口经过实际运行验证后采用：

- 00:00–05:59：`beginHour = 12:00`
- 06:00–23:59：`beginHour = 当前整点`

FORECAST 和 ROOM_STATUS 共用该窗口逻辑。

ROOM_STATUS 在凌晨使用 12:00 查询窗口时，数据库中的 `snapshot_hour` 仍记录真实采集时间；不要因为两者不同就自动判定为错误。

### PMS 旧数据保护

如果某报表本轮没有生成新的 JSON，`fetch_main.py` 会把该报表视为失败并跳过 ETL，避免旧文件重复入库。不要为了“让任务继续成功”而绕过这层保护。

### JD01 空结果是合法情况

JD01 本轮返回 0 条预订可以是正常业务结果。ETL 必须允许空列表正常完成，不应假设一定存在 `data_list[0]`。

### PMS 全量采集的网络保护

正常全量 PMS 采集包含：

- 报表之间随机等待 2–5 秒。
- 连续 3 个报表失败后进行一次报表中心连通性探测。
- 探测失败或返回 5xx 时停止继续访问本轮剩余报表，并将其视为未采集。
- 手工 `--reports` 模式不启用全局熔断，便于调试和补采，但多报表执行仍保留间隔。

不要把连接层 `ERR_CONNECTION_CLOSED` / TLS EOF 默认解释成登录失效。

### 美团页面型任务冷却

当前主动降低以下页面型任务访问频率：

- 美团促销开通状态：随机 10–14 小时。
- 美团视频上传状态：随机 22–26 小时。
- 美团近 30 天推广效果：随机 22–26 小时。

冷却语义必须保持：

- 冷却期内的定时触发只跳过，不刷新冷却时间。
- 真正开始执行时只记录尝试时间，不提前生成新冷却。
- 真正成功写入数据库后生成下一次冷却。
- 真正执行失败后也生成下一次冷却，防止连续重试。
- `--force` 可以绕过当前冷却，但实际成功或失败后仍应重新生成冷却。

### 美团风险控制处理原则

项目曾遇到美团推广页面正常人工点击也进入风险控制的情况。处理原则：

- 不实现绕过风控、挑战、指纹或限流的方案。
- 不增加循环刷新、高频重试或自动反复点击。
- 可以降低访问频率、安全失败、记录诊断信息，并等待正常人工访问恢复。

### 已确认的其他设计

以下行为属于当前有意设计，不要在每次 review 中反复当作缺陷提出：

- `ctrip_order_loss_monthly` 作为当前快照/上月聚合排名使用，现有唯一键与同步方式可接受。
- 评价回复调度读取 `price_scheduler.interval_minutes` 属于当前设计。
- 登录或切换账号后自动识别酒店参数失败时，保留旧参数并告警，而不是清空配置。
- 美团部分页面数据依赖页面产生的动态签名请求；不要轻易退回固定旧签名 URL。
- 公共浏览器锁用于避免人工/定时任务同时占用同一 Edge profile。

## 6. Git 和修改流程

默认分支：`master`。

一般开发流程：

1. 重新读取当前 `master`。
2. 从最新 HEAD 创建独立分支。
3. 只修改与当前需求直接相关的文件。
4. 创建 Pull Request，并说明行为变化和未改变的范围。
5. 用户确认“合并”后再执行 merge，除非用户明确要求直接修改 `master`。
6. 合并前重新获取 PR 状态和 head SHA，避免基于过期分支状态操作。

不要声称 CI/测试已通过，除非本轮确实检查或执行过对应 CI/测试。

## 7. 配置与安全

不要提交真实凭证或运行态文件，包括但不限于：

- `OTA采集服务/config/settings.json`
- Cookie、密码、Token、签名 URL、PMS 会话
- `%LOCALAPPDATA%/HotelAgent/` 下的浏览器 profile 和任务 state
- 日志、采集输出、真实客户数据
- `runtime/` 便携运行环境

仓库只应保留脱敏示例和代码。

## 8. 常用入口

Windows 便携环境常用命令：

```powershell
runtime\python.exe "OTA采集服务/runner.py" status
runtime\python.exe "OTA采集服务/runner.py" run-once
runtime\python.exe "OTA采集服务/runner.py" run-task meituan_scan_order
runtime\python.exe "OTA采集服务/runner.py" run-task pms_fetch
```

PMS 手工指定报表时查看 `fetch_main.py --help`，不要假设全量采集和手工补采使用完全相同的熔断策略。

## 9. Review 关注点

优先关注：

- 是否会写入错误酒店或错误日期的数据。
- 本轮采集失败后是否可能误用旧文件。
- 页面/接口失败是否会形成高频重试。
- 数据库写入是否保持当前唯一性和快照语义。
- 调度、登录态、Edge profile 是否存在并发冲突。
- 修改是否改变已经确认的业务规则。

除非当前需求直接相关，不要把“测试覆盖不足”或旧数据库迁移兼容作为整体 review 的主要问题。

## 10. 文档维护

出现以下变化时同步更新 `docs/PROJECT_STATUS.md`：

- 新增/删除采集任务或 PMS 报表。
- 数据库实体表数量或核心 Schema 明显变化。
- 重要业务规则发生改变。
- 解决一个会影响后续排查的重要稳定性问题。
- 新增明确的下一阶段工作目标。

`AGENTS.md` 只维护相对长期的规则，短期故障、近期提交和阶段进度放到 `docs/PROJECT_STATUS.md`。